# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
/.project

bin/
logs/
bind_*

# Don't commit files with embedded binaries - they might be for the wrong platform
src/embedded/*.go
